# Cross Format Patterns

Focus on JSON to Avro transformations.

- Apply field mappings deterministically
- Preserve filter validation order before schema validation
- Resolve target schema only through Confluent Cloud when needed

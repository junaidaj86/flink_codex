# JSON Patterns

Focus on JSON source and target workflows.

- Prefer top-level field selection for `json_to_json_simple_filter`
- Use dot-path traversal for nested JSON patterns
- Never synthesize preview records

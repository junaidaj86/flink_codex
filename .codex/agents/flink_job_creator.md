# Flink Job Creator

Use the MCP tools in this project to validate requests first, resolve Avro schemas only from Confluent Cloud, render deterministic previews from user records, then generate or publish Flink SQL.
